{
  "version": 2,
  "buildCommand": "cd client && npm install --legacy-peer-deps && npm run build:vercel && cd .. && mkdir -p public && cp -r client/build/* public/",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/public/$1"
    }
  ]
}